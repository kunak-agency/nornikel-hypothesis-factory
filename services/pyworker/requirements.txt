fastapi==0.115.6
uvicorn[standard]==0.32.1
docling==2.108.0
# sentence-transformers (not FlagEmbedding): FlagEmbedding 1.3.4 eagerly imports
# decoder-only Gemma-based reranker code at import time that breaks against
# current transformers internals, even though we only need encoder-only
# BGE-M3 / bge-reranker-v2-m3.
sentence-transformers>=3.3
pydantic==2.10.3
python-multipart==0.0.20
